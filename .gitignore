# ══════════════════════════════════════════════════════════════════════
# Solo el codigo necesario para CORRER el app se pushea a Hookspa/TMPOS.
# Todo documento/artefacto interno queda LOCAL (uso interno).
# git NO soporta comentarios inline: cada # va en su propia linea.
# En vivo queda: app.html, index.html, ver.html, report.html, css/, js/,
#   refs_02.csv, logo_exports/, screenshots/, supabase/, .github/,
#   README.md, .gitignore
# ══════════════════════════════════════════════════════════════════════

# Sistema / herramientas
.claude/
.gstack/
.DS_Store

# Backups JSON (artistos-backup-*.json).
# OJO: ignora tambien package.json/manifest.json futuros -> whitelistealos con !package.json
*.json
!package.json
!package-lock.json

# Infraestructura de pruebas de regresión (runtime público, sin artefactos locales)
node_modules/
test-results/
playwright-report/
blob-report/
_site/
_lkg/
_incident/

# Entregables estrategicos y de negocio - NUNCA al repo publico
# (investor deck, modelo financiero, CEO/Council reviews, specs)
research/

# Previews/mockups de diseno internos
design/

# Documentos internos de trabajo (no necesarios para correr el app)
HANDOFF.md
CHANGELOG.md
DESIGN.md
AGENTS.md
CLAUDE.md
PRODUCT.md
CONTEXT.md
docs/
.scratch/

# Contexto de /impeccable (config local de live mode)
.impeccable/

# Scripts one-off de dev (rehost de miniaturas, downloader IG) - no runtime
tools/

# Estado local del CLI de Supabase (project-ref, pooler-url con credenciales)
# NUNCA al repo publico: supabase/ si se pushea, este subdirectorio no.
supabase/.temp/
